{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "rm -rf tests/src/* && cp ../src/* tests/src/ && node --test tests/*.test.js",
    "test:only": "rm -rf tests/src/* && cp ../src/* tests/src/ && node --test-only --test tests/*.test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@deutschesoft/awml": "^2.2.6",
    "@testing-library/react": "^14.1.2",
    "global-jsdom": "^9.1.0",
    "react": "^18.2.0"
  }
}
